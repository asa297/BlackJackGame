import axios from "axios";

// export const PlayGame = async () => {
//   const { data } = await axios.get("/api/game");
//   return data.player_cards;
// };

export const CalulatePoint = data => {
  // socket.emit("createroom", data);
};
